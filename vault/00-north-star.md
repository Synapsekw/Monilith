---
type: north-star
status: active
last-updated: 2026-08-26-0900
tags: [project/monolith, north-star]
related:
  - "[[README]]"
  - "[[product]]"
---

# Monolith — North Star

> Single canonical entry point. Where are we, where are we going, why. Open this first.
> **When state changes, update the relevant section and bump `last-updated` in the frontmatter.**
> Keep this concise — phase detail lives in [[platform-roadmap]], history in `vault/sessions/`.

## 1. Pitch

**Monolith** is a cloud-native **"Work OS"** in the spirit of Monday.com — folding in the best of
ClickUp (nested hierarchy, docs, native time tracking) and Asana (goals/OKRs, workload, portfolios)
into one coherent product. Not a clone: the _ultimate_ version. Monday's visual, color-coded board
experience as the foundation; ClickUp's depth; Asana's polish. Design language: modern monochromatic
(neutral grayscale) with a single configurable accent, **dark-first** ("Monolith Keystone": layered near-black surfaces +
periwinkle accent; light supported but secondary), Linear-grade restraint applied to a colorful category.
Built on **Next.js 16 + React 19 + Tailwind v4 + Supabase**, multi-tenant (org-scoped RLS) from day one.

Master spec: [[2026-06-14-pulse-design]] (`docs/superpowers/specs/2026-06-14-pulse-design.md`).
Visual reskin target + prototype reuse map: [[2026-06-16-decision-08-dark-first-monday-reskin]].

## 2. Product north star — phased build (0 → 9)

From the master spec §7 — **status + one-line outcome only**. Per-slice detail and full history live in
[[platform-roadmap]] and the session notes. **Commit + checkpoint after each phase; run tests +
advisors + regenerate types before moving on.**

- **0 — Setup** — <span style="color:#22c55e">**[Done]**</span> — Scaffold, deps, theming tokens, Supabase + MCP wired; themed shell + dark/light toggle + ⌘K stub. ([[2026-06-14-phase0-setup]])
- **1 — Auth & tenancy** — <span style="color:#22c55e">**[Done]**</span> — Email/password auth, org creation + membership, protected routes, RLS baseline. ([[2026-06-15-phase1-auth-tenancy]])
- **2 — Boards core** — <span style="color:#22c55e">**[Done]**</span> — Workspaces→boards→groups→items, Table view (6 column kinds), inline editing, optimistic updates, realtime. _(2a schema+Table · 2b inline editors · 2c column management)_
- **3 — Views** — <span style="color:#22c55e">**[Done]**</span> — Kanban + Calendar + Timeline/Gantt with dependencies; view switcher + saved config (client-side, no RSC refetch).
- **4 — Collaboration** — <span style="color:#22c55e">**[Done]**</span> — Item detail panel, updates/comments/@mentions, attachments, activity log, notifications inbox. _(4a panel+updates+activity · 4b mentions+notifications · 4c attachments — spec [[2026-06-16-phase-4-collaboration-design]])_
- **5 — Automations + Rules** — <span style="color:#22c55e">**[Done]**</span> — Trigger/condition/action builder on Postgres triggers + `pg_cron`/`pg_net` (no Edge Functions); recipes, run-history, webhook actions. _(5a → 5c-2 + move_to_group)_
- **6 — ClickUp depth** — <span style="color:#eab308">**[In progress — 6a–6h done; 6e Docs deferred]**</span> — Subitems, custom fields/statuses, time tracking, relations + mirror columns + aggregation, workspace management, real-time presence. 6e Docs deferred ([[2026-06-21-decision-24-defer-phase-6e-docs]]: too complex + not fully cloud-native).
- **7 — Asana polish** — <span style="color:#22c55e">**[Done — 7a + 7b + 7c + time-allocation]**</span> (Workload v3 variance/drill-down is optional future depth) — Portfolios, Goals/OKRs, Workload/capacity (v2: workspace/board filtering + planned/actual metric; v3: variance + per-day actuals drill-down). Time allocation: `/time` weekly "My Time" card (manual decimal-hours per task/category/day, save-as-you-go) unified with timers into one actuals ledger; Workload reworked to full-canvas (utilization % + capacity bars). ([[2026-06-23-2059-time-allocation-my-time-card]])
- **8 — Dashboards + templates + ⌘K polish** — <span style="color:#22c55e">**[Done + v2 polish + AI gen]**</span> — Cross-board widgets (Number/Chart/Battery/List + filter), board templates, ⌘K nav + create. v2 polish: 9 chart types (line/area/stacked/grouped/combo/donut/radial) via `dashboard_series` (date-bucket + multi-series; group by date/status/dropdown/people), unified edit drawer w/ live preview, bordered-card reskin. **AI generation:** Opus 4.8 reads a board's schema+stats (no raw cells) and proposes a full dashboard via a Generate-with-AI wizard + Keep/Discard/Regenerate review banner. **Chart engine reskin:** `ChartWidget` moved onto shadcn chart primitives (P1) then an expressive "Direction C" restyle (P2) — spectrum-hero for single metrics, categorical palette for real multi-series, configured colors preserved (data layer stops inventing color), gradients + Signature motion. ([[2026-06-23-1953-dashboards-polish-v2]], [[2026-06-24-0912-ai-dashboard-generation]], [[2026-07-05-2018-shadcn-charts-phase1-2-expressive]])
- **9 — Hardening & Optimization** — <span style="color:#22c55e">**[Done — 9.1–9.6 + audit-fix sweep]**</span> — Perf + perceived-perf program (Track A actual speed, Track B perceived speed). 9.1 auth fast-path + 9.2 streaming shell + 9.3 tagged `use cache` shell reads + **9.3b widget-aggregation cache** + 9.4 route skeletons + 9.5a interaction responsiveness + **9.6 Web-Vitals gate** all shipped. **Audit-fix sweep (2026-07-05):** 4-agent audit → 11 parallel fix branches — intra-org RLS leak on dashboard RPCs closed, SSRF/open-redirect/enumeration/CSV-injection fixed, hot-path `revalidatePath` + presence re-render storm removed, realtime self-heal, silent-failure surfacing, import/timezone correctness. Spec `docs/superpowers/specs/2026-06-22-phase-9-performance-optimization-design.md`. ([[2026-06-28-1743-phase-9-close-parallel-batch]], [[2026-07-05-0811-audit-fix-sweep]])
- **RS — Design refresh (dark-first reskin)** — <span style="color:#22c55e">**[Done — dark + light]**</span> — Dark-first near-black palette as `.dark` OKLch tokens (dark default); light-mode pass shipped. ([[2026-06-16-decision-08-dark-first-monday-reskin]])
- **MVP-F — MVP Final Features (user-feedback backlog)** — <span style="color:#22c55e">**[Done — 9/9 shipped to prod]**</span> — All open in-app feature requests built and promoted same-day (`docs/superpowers/plans/2026-07-03-mvp-final-features.md`, `/goal`). Batch A: Excel export formatting, calendar/timeline quick-edit peek, overdue tint + percent sync, currency + dirham sign, column reorder, completion widget. Batch B: per-group summary rows, priority + auto-critical, health widget + weekly digest. ([[2026-07-03-1154-mvp-final-features-wave]], [[2026-07-03-1512-mvp-final-batch-b-promote]])
- **10 — AI & Agents** — <span style="color:#eab308">**[In progress — E1 + Batch 2 (E2/E3/E4) + E5 in prod (E5 inert until prod Vault is provisioned); E6 open]**</span> — Reusable AI platform layer + feature wave, sold two ways (managed **or** BYO). **E1 complete (2026-07-11, reconciled hybrid model):** `ai_mode = off|managed|org_byo|per_user` (default `per_user` — the shipped per-user store kept as an explicit mode), one gateway (`resolveAiAdapter`/`runAi`) metering every call into `ai_usage`, entitlements w/ managed credit ceiling, org Settings AI card + platform-admin plan lever, dashboard-gen migrated onto the gateway, and **Ask Monolith** (workspace NL Q&A via RLS-scoped read tools, Anthropic-gated, 6-round tool loop, ⌘K + header entry). Client write path to `org_ai_settings` removed (entitlements are platform-controlled). Reconciliation spec `docs/superpowers/specs/2026-07-11-ai-e1-scope-reconciliation-design.md`. **E5 agentic automations + semantic search — done on `develop`, not yet promoted (built ~2026-07-21):** F13 AI action step in automations (decide loop + confined apply + dry-run), F14 Autopilot scheduled board agent (bot identity + bounded housekeeping), F15 pgvector semantic search (`match_items` RPC + find-similar). Guardrails: [[2026-07-20-decision-29-agentic-automation-guardrails]]. Build session: [[2026-07-21-1123-e5-agentic-semantic-full-build]] — written on a different local checkout and only landed in this vault on 2026-07-25, which is why earlier revisions of this doc recorded it as missing. _Embeddings still need a backfill (`POST /api/ai/embed?mode=backfill`) before semantic surfaces return results in prod._ **Open epics: E6 Stripe only** — E2/E3/E4 shipped to prod via #62 (an earlier revision of this bullet contradicted its own status badge by listing them as open). **Ask Monolith Phase 2 shipped 2026-07-27** — `/ask` now proposes writes and executes them behind a confirm card: the turn ends at the card, the proposal persists in `ai_messages.tool_trace` (no migration), and Approve/Cancel append an outcome turn because `ai_messages` has no UPDATE policy ([[2026-07-27-0659-batch-a-builds-conformance-probes]]). **E6 is now the only open AI epic.** **The provider & model layer (Spec 1 of 3) shipped to production 2026-08-11 (PR #95)** — DB-driven `ai_providers` + `ai_models`, one key per provider across five providers, agents pinnable to any reachable model, and a daily gateway-feed refresh so new models need no deploy ([[2026-08-11-1501-provider-model-layer-spec-1]], [[2026-08-11-decision-35-the-gateway-id-namespace-is-not-the-providers]]). **The agent runtime (Spec 2a) shipped to production 2026-08-14 (PR #96)** — agents are a bounded, capability-gated tool loop that can act on boards, with ungranted calls recorded as durable proposals; existing agents unchanged by construction (`capabilities` defaults `'{}'`) ([[2026-08-14-0808-agent-runtime-spec-2a]], [[2026-08-14-gotcha-91-a-guard-written-for-a-human-actor-does-not-survive-an-ai-actor]]). **Spec 2b (agent knowledge) is next, then Spec 3 (orchestration + `@handles`).** **Personal Agents Phase 2 (board thread dock) shipped 2026-08-04** — board-scoped agent conversations, private by default and shareable to the board, plus a fourth write verb (`propose_move_item`) ([[2026-08-04-1443-board-dock-and-ai-move-verb]], [[2026-08-04-decision-33-a-board-dock-reverses-ask-as-a-standalone-surface]]). **The write path was then made actually usable** ([[2026-08-04-1907-promote-and-ai-write-followups]]): every verb is now reachable (`query_items` emits item ids, so the path no longer depends solely on semantic search) and an approved write renders on the board with no reload, via a transient `BoardEffect` folded into the client cache. **MCP server (agent connectivity) shipped 2026-07-24:** hosted remote MCP server (`/api/mcp`) + from-scratch OAuth 2.1 authorization server so Claude Desktop/claude.ai can connect as a real Monolith user and read/write board items through a genuinely RLS-scoped session (no service-role bypass) — 6 tools (list/get boards+items, create/update item, no delete), Settings → Connected Apps. Built subagent-driven, 14-task plan; final whole-branch review caught and fixed a real concurrency bug in the session-bridge before merge ([[2026-07-24-gotcha-56-per-request-session-refresh-race]]). Spec/plan: `docs/superpowers/specs/2026-07-24-mcp-server-design.md`, `docs/superpowers/plans/2026-07-24-mcp-server.md`. ([[2026-07-24-1950-mcp-server-oauth]]) **In-app setup guide shipped 2026-07-25** as part of the settings redesign — server URL, per-client steps, tool/permission table, revoke ([[2026-07-25-1056-settings-redesign-mcp-guide]]). **Ask Monolith full-page (shipped to prod 2026-07-17 as "Ask AI"):** owner-approved expansion promoting the Ask Monolith popup to a standalone `/ask` chat page — persisted per-user cross-board history, multi-turn (rolling summary), token streaming (Phase 1 = E1 F5 expansion), then confirm-before-execute write actions (Phase 2 = E3 F6 pulled forward). Deliberately reverses the "AI at the seams, no standalone chat" stance ([[2026-07-12-decision-27-ask-becomes-standalone-surface]]). Spec `docs/superpowers/specs/2026-07-12-ask-pulse-full-page-conversational-design.md`, Phase-1 plan `docs/superpowers/plans/2026-07-12-ask-pulse-full-page-conversational.md`. ([[2026-07-11-2116-ai-e1-hybrid-gateway-ask-pulse]], [[2026-07-05-decision-26-ai-platform-dual-billing]])
- **TOUCH — iPad optimization** — <span style="color:#22c55e">**[Done — Batch 1 + Batch 2 8/8 surfaces]**</span> — iPad-first, full authoring parity, touch-ergonomics (no layout reflow). Batch 1 shared primitives (`useCoarsePointer`, touch dnd sensors, `<DragHandle>`, `<RevealOnHover>`, 44px targets, touch-aware tooltip) + all 8 Batch 2 surfaces shipped: Table, Item Panel, Nav, Kanban, Gantt, Calendar, Dashboard canvas, Command palette/menus. Spec `docs/superpowers/specs/2026-06-26-ipad-touch-optimization-design.md` + per-surface specs `2026-06-29-touch-batch2-*`. Deferred: phone reflow, PWA/offline, iPad E2E matrix. ([[2026-06-28-1822-ipad-touch-foundation]], [[2026-07-02-1218-quality-triage-promote-43-scoping]])
- **PF — Polish & Fluidity (perf follow-on)** — <span style="color:#22c55e">**[Shipped to prod 2026-07-17 — A4 skipped]**</span> — Second perf pass beyond Phase 9's program, from a 4-dimension scan (rendering/caching · data-fetching · client smoothness · bundle). Baseline confirmed strong; this closes the remaining **targeted gaps** as 4 independent worktree batches: **A — server latency** (parallelize board/dashboard payload head-reads, batch the `/home` dispatch + last-board cookie fast path, `/my-work` + `/boards` route skeletons, collapse the My Work 4-phase chain into one SECURITY-INVOKER RPC); **B — board interaction** (debounce+`useDeferredValue` quick-search, hoist dependents-map + memoize row/cell components, virtualize Gantt rows, clamp Calendar agenda); **C — bundle** (lazy `@dnd-kit` out of the shell, lazy `DashboardCanvas`/react-grid-layout, working Turbopack bundle-analyze, bound sidebar lists, landing rAF resize); **D — polish** (kill per-field `router.refresh()` in Goal drawer + TimeCard, stream timezone so content never blanks, transformed thumbnails, truthful `unstable_instant` comment per [[2026-07-04-gotcha-48-unstable-instant-blocked-by-shell-searchparams]]). Plan: `docs/superpowers/plans/2026-07-09-perf-polish-fluidity.md` (20 TDD tasks, execution DAG, per-batch manual test guide). Disjoint from Phase 10 (AI) — can run in parallel.

## 3. Now

- **⚠️ ENVIRONMENT — the production deployment runs the DEV database.** `www.monolith.works` is
  wired to the **DEV** Supabase project (`hjqca…`); the PROD project (`jzsyq…`) is provisioned and
  mirrored but serves no traffic. This is deliberate and stands **until the app is declared
  feature-complete**, when we cut over. So: DEV holds the real live user data (treat it with
  production care), and inspecting `jzsyq…` will never explain live behaviour.
  Full note: [[2026-08-02-decision-32-production-runs-the-dev-database]].
- **Phase:** **Phase 10 (AI & Agents) — agents can ACT, and now they KNOW, safely.** **Spec 2b (reference documents) is LIVE IN PRODUCTION** (promoted 2026-08-26, PR #99; [[2026-08-26-0900-carryover-batch-and-promote-99]]): a **personal** library of text — pasted, or extracted from `.md`/`.txt`/`.pdf`/`.docx`/`.xlsx` — attached many-to-many to an owner's agents and injected **verbatim** into the system prompt inside the existing cached system message. **No retrieval, deliberately**: a structural example must arrive whole and a policy list complete, so the correct retrieval result is always "all of it". The ceiling is enforced at **attach time** by a live meter, and **nothing truncates** — an over-budget set is dropped entirely and the run is flagged `documents_omitted`, following the `model_substituted` precedent that a run which worked without its documents *succeeded*. `.xlsx` alone parses **server-side**, because `parseWorkbookSheets` owns the zip-bomb guard. The two build-time lessons are [[2026-08-25-gotcha-93-a-preview-that-shares-a-function-but-not-its-inputs-still-lies]] and [[2026-08-25-gotcha-94-a-plan-is-right-about-intent-and-wrong-about-signatures]]. **Shipped in the same promotion:** a per-agent stable nonce (`user_agents.doc_nonce`) keys the instructions delimiter whenever documents are attached, closing a forgery gap in the sentinel defense without breaking Anthropic prompt-cache reuse (same agent → same nonce → same bytes, every run); the sentinel itself was narrowed to stop false-positives on documents containing the literal heading "REFERENCE DOCUMENTS"; a real production bug where the four aggregate dashboard widgets (number/battery/completion/health) were resolving through the service client and raising `42501` since 2026-07-04 is fixed; and **F17 — usage dashboard + AI weekly-digest narrative** shipped (monthly usage rollups, a metered/gated narrative generator, a breakdown card on `/settings/ai`, the narrative surfaced in the weekly digest and notifications). **Spec 2a (agent runtime) is LIVE IN PRODUCTION** (promoted 2026-08-14, PR #96; [[2026-08-14-0808-agent-runtime-spec-2a]]). A personal agent is no longer a fixed briefing pipeline: `personal-agent/route.ts` runs `generateText({tools, toolApproval, stopWhen: stepCountIs(12)})` over descriptors exported by the same 24 MCP tool handlers, so **one definition serves both transports**, plus two agent-only tools (`create_file`, `create_automation`). Per-agent **capability grants** (`board.write`, `files.write`, `automation.create`, `time.log`) intersect an **org ceiling at run time**; anything ungranted is **denied in-loop and written as a durable `user_agent_proposals` row so the unattended run still finishes** — never suspended. `user_agents.capabilities` defaults `'{}'`, so every pre-existing agent stays exactly as read-only as it was: the relaxation is opt-in **by construction, not by vigilance**. Three additive migrations, ledger **141/141**. The security lesson is [[2026-08-14-gotcha-91-a-guard-written-for-a-human-actor-does-not-survive-an-ai-actor]] — the final whole-branch review found `create_automation` handing the model `call_webhook` (model-chosen URL **and** auth header), the one action the codebase had **already** ruled AI may never choose; the org-admin gate survived the extraction intact and was simply never a defence, because it authorises the *actor* while a model's actor is trusted by construction. Personal Agents Phase 2's board thread dock shipped and is **in production** ([[2026-08-04-1443-board-dock-and-ai-move-verb]]): a collapsible, resizable right dock hosting agent conversations, private by default and shareable to the board, with scheduled briefings landing as repliable threads and **zero requests until opened**. A permanent dock is chrome, reversing decision-27 ([[2026-08-04-decision-33-a-board-dock-reverses-ask-as-a-standalone-surface]]). Four AI write verbs exist (`create_item`, `move_item`, `set_item_fields`, `create_group`); as of **PR #84** they can all be **reached in production** (ids come from `query_items`, not only semantic search) and their results **render without a reload** — the gap between "fixed on `develop`" and "fixed for users" is closed ([[2026-08-04-2222-promote-84-ai-write-path-live]]). The cron chain remains live — E5, F13, F14, the weekly digest and agent briefings are all reachable in production. **E6 Stripe is the only open AI epic.**
- **Branch:** **`develop` @ `d6d925b7` == `main` @ `821efc9a` == production** (promoted 2026-08-26, **PR #99**, 52 commits — Spec 2b reference documents + a same-session carryover batch: the per-agent nonce defense, sentinel narrowing, the dashboard-widget RLS fix, F17 usage/digest, the DocumentPicker cap fix, an a11y pass, migration cleanup, and two test-infra fixes). Verified live, not assumed: Vercel commit-status `state=success`, `www.monolith.works` **200**, `/settings/ai` **307**. Ledger **146/146**. Squash divergence healed (`57b77edb`; `origin/main` verified an ancestor of `origin/develop`, heal tree byte-identical). **Two `/updates` gaps the date-bucket coverage check couldn't see** — F17 and the dashboard fix shipped 2026-08-25, the same day as an announcement commit that only covered Spec 2b's own three entries, so the day read "covered" while those two features weren't; same shape for the DocumentPicker + a11y fixes on 2026-08-26. Backfilled with two more dated announcement commits (`ee7de01e`, `80b6811f`) — **the mechanical check proves a trailer exists that day, never that it describes what actually shipped that day; only remembering what you built catches that.** No live worktrees. Preceded by **`main` @ `461cadb0`** (2026-08-14, **PR #96**, 37 commits — the agent runtime, Spec 2a). Verified live, not assumed: Vercel `state=success` on `461cadb0`, `www.monolith.works` **200**, `/settings/agents` **307**, and `/updates` serving **all five** new agent entries — the changelog is the cheapest proof the new build is the one being served. Ledger **141/141**. Squash divergence healed (`6769421f`; `origin/main` verified an ancestor of `origin/develop`, heal tree byte-identical). Unpromoted delta **0**. **No live worktrees.** Preceded by **`develop` @ `a0a692df` == `main` @ `1ebc8a44` == production** (promoted 2026-08-11, **PR #95**, 47 commits — the AI provider & model layer, Spec 1 of 3, plus the test-account-hygiene merge). Verified live, not assumed: Vercel `state=success` on `1ebc8a44`, `www.monolith.works` **200**, `/settings/ai` **307**, `/updates` serving **all three** new AI entries — and `/api/ai/models/refresh` returning **401, not 404**, which is the proof that matters: the daily catalog cron finally has a live HMAC-gated target instead of a missing route. `AI_PGNET_HMAC_SECRET` confirmed present in the Vercel production env *before* merging. Squash divergence healed (`a0a692df`; `origin/main` verified an ancestor of `origin/develop`, heal tree byte-identical). Ledger **138/138**. Delta computed from the heal watermark `f87b0d55` — the raw `origin/main..origin/develop` count read **2579**, which is the squash-plus-heal accumulation, not work. Preceded by `main` @ `65f4a5d1` (promoted 2026-08-09 15:36 UTC, **PR #91**, +6602/−421 — the attachment preview viewer, the desktop manual-update path, the desktop 1.0.1 contract bump and the dev service-worker fix). Verified live, not assumed: **Vercel `state=success`** on `65f4a5d1`, `www.monolith.works` 200, and `/desktop-release.json` serving **`latestShell: 1.0.1`** with both 1.0.1 DMG URLs resolving (206) — plus the *shipped* `checkVersionGate()` run against production returning `{supported: true, latest: "1.0.1"}`, which is the actual code path, not a curl standing in for it. **`develop` @ `594413d3` == `main` @ `896507ad` == production** (promoted 2026-08-09 22:0x UTC, **PR #93**, 9 commits — Report Builder v2). Verified live, not assumed: Vercel `state=success` on `896507ad`, `www.monolith.works` 200, `/reports` **307** (the route exists behind auth, not a 404) and `/updates` serving all three new report entries — the changelog is the cheapest proof the *new build* is the one being served. The 25-entry `/updates` backfill shipped separately as **PR #92** (`fb9fc5c7`) earlier the same day. Squash divergence healed (`594413d3`, tree verified byte-identical; `origin/main` IS an ancestor of `origin/develop`). Preceded by **PR #89** (`7a9ab15e` — invitation realtime, MCP attachments, the desktop download; [[2026-08-09-1702-desktop-drag-fix-and-pr89-promote]]), **PR #88** (`55153248`, 2026-08-06; [[2026-08-06-1343-mcp-full-surface-22-tools]]), **PR #86** (`3f3f55b1`) and **PR #85** (`0bfa9967`; [[2026-08-06-0902-mcp-oauth-loopback-and-bridge-secret]]). Squash divergence healed with `-s ours` after each (`8d49a039` latest — verified `origin/main` IS an ancestor of `origin/develop` and the heal's tree byte-identical to develop's tip) ([[2026-06-30-gotcha-32-squash-merge-breaks-develop-main-ancestry]]) — do this **every** promotion or the next PR shows `CONFLICTING`. **The PR head moved while #89 was open** — `task/mcp-metadata-attachments` merged mid-flight, so GitHub merged `753765a2` and 35 commits shipped against a body describing 27; the body was corrected after the fact and `/promote` now re-reads `headRefOid` before the gate. A `cancelled` CI conclusion during that window meant "a newer commit superseded this run", **not** a failure. Ledger **135/135 in sync** after the **eighth** consecutive `apply_migration` mis-stamp + reconcile — it is routine, budget it. **No live worktrees** — the main checkout is the only one. Origin still carries **8 dependabot branches** (2 GitHub-Actions, 6 npm) and `vercel/install-and-configure-vercel-s-cz9g1t`; the npm ones will contend with E6's Stripe SDK install over the lockfile, so sequence them either side of that track, never during.
- **`vercel.json` now restricts auto-deploy to `main`** (`deploymentEnabled: {"**": false, "main": true}`, landed 2026-08-06 from a parallel session). Preview builds no longer run on `develop` or on promotion PRs — so **a `develop → main` PR no longer carries a Vercel preview check**, and that signal is gone from the pre-merge evidence. Production deploys are unaffected.
- **The MCP OAuth server worked for exactly one client, and that hid two structural bugs** ([[2026-08-06-0902-mcp-oauth-loopback-and-bridge-secret]]). `claude.ai` registers one fixed https callback and is the only client it had ever seen, so (a) exact `redirect_uris.includes()` matching and (b) a Vault secret named `mcp_bridge:<userId>` both passed every test and four months of production. Connecting a CLI client exposed both: exact matching rejects any client that binds an **ephemeral loopback port**, which RFC 8252 §7.3 says a server MUST allow ([[2026-08-06-gotcha-76-exact-redirect-uri-matching-locks-out-every-cli-client]]); and a per-user secret name means the **first** bridge a user mints squats it forever, so no second client — and no MCP client alongside a personal agent — could ever connect ([[2026-08-06-gotcha-77-a-per-user-vault-secret-name-caps-a-user-at-one-bridge]]). The second surfaced as a **500 with an empty body**, diagnosable only from the Vercel runtime log; the token endpoint now returns the RFC 6749 §5.2 `server_error` envelope. Both verified live: two token rows with distinct bridges for one user.
- **`/promote`'s commitlint gate — FIXED 2026-08-09** (`f0fb12ff`, `309484ac`; [[2026-08-09-gotcha-82-squash-plus-ours-heal-makes-the-main-develop-range-grow-forever]]). It had cost **three** promotions plus two more workarounds. Root cause is structural and worth keeping: squash-merging to `main` and healing with `-s ours` leaves every pre-squash commit permanently unreachable from `main`, so `origin/main..origin/develop` **accumulates forever** — 2448 commits on 2026-08-07 against a real delta of 35. Linting it re-judged Phase 6/7-era commits against a commitlint config that postdates them, on a job CI deliberately skips for `base_ref == main`. The command now derives `PROMO_BASE` from the most recent heal commit and uses it for commitlint, the count, the PR body **and** the emptiness check — which could never fire before, because after a heal `develop`'s tip is the heal commit and the range is non-empty even when nothing is unpromoted. **`git rev-list --count origin/main..origin/develop` is not a measure of unpromoted work in this repo and never will be again.**
- **A shipped verb is not a reachable verb.** `propose_move_item` passed three reviews and four green gates on 2026-08-03 and was **dead on arrival** — the owner hit it within the day. Its id had to come from `semantic_search_items`, which is entitlement-gated, returns `[]` on any error, and needs the embedding sweep to have indexed the item. `query_items` had `item.id` in hand and dropped it; `propose_set_item_fields` named **no** id source at all. This is [[2026-08-04-gotcha-73-a-tool-description-is-untested-surface-and-can-ship-a-dead-verb]] one layer down, and the durable defence is `id-sources.test.ts`: for every (verb, required id, named read tool) it runs the **real handler** and asserts the id is in the JSON the model receives.
- **`revalidatePath` was never the missing piece.** The recorded diagnosis for "an approved AI write is invisible until you reload" blamed its absence in `src/lib/boards/actions/item.ts`. That absence is **deliberate** — the board hydrates once via `initialData` + `staleTime: Infinity` and never refetches the RSC, so revalidation changes nothing on screen and costs 9 queries (the rule is written down at `src/lib/boards/actions/group.ts`). The fix was to finish a rollout the repo had already decided on ([[2026-06-17-gotcha-13-realtime-only-insert-needs-optimistic-echo]]): `executeAction` returns a transient `BoardEffect`, one hook folds it into the board's TanStack cache. Effects deliberately stay **out** of `ExecutionResult`, which is persisted into `ai_messages.tool_trace` and would replay stale rows onto the board on every thread reopen.
- **A repair that matches zero rows reports success** ([[2026-08-04-gotcha-75-a-zero-row-repair-reports-success]]). `apply_migration` mis-stamping is now **5 for 5** — it is routine, budget it. `reconcile-migration-version.sh`, the sanctioned fix, **used to** print `WHERE name = '<slug>'` while the row it targets stores the full stamped filename: the `UPDATE` touched nothing, exited 0, and looked repaired. Only `pnpm db:ledger-check` caught it. **Repaired 2026-08-04** — keyed on `version` alone with `RETURNING`, and STEP 4 makes `db:ledger-check` mandatory (see Owed — tooling). The generalisable lesson survives the fix: **a `WHERE` clause is an assumption, and an `UPDATE` that reports rows-affected nowhere cannot tell you the assumption was wrong.**
- **A CHECK cannot express "this board is in my org."** A CHECK cannot subquery and must be IMMUTABLE, so it is never re-evaluated when the referenced row changes. The coupling is a **composite FK** `(board_id, org_id) → boards (id, org_id)` — enforced for every role including `service_role`, which bypasses RLS but never a constraint ([[2026-08-04-decision-34-a-docked-thread-belongs-to-its-boards-org]]). Two details are load-bearing: `MATCH SIMPLE` keeps `board_id IS NULL` legal as a property of the constraint class rather than a line someone must remember, and the PG15+ **column-list** `ON DELETE SET NULL (board_id)` is required — the bare form nulls every referencing column including the `NOT NULL org_id`, breaking `purgeBoard`. It was reachable with **no attack**: the board page never reconciles the `pulse_active_org` cookie, and 8 DEV users hold multi-org membership.
- **The whole-branch review keeps finding what per-task reviews cannot.** Three sessions running. Subagents were therefore instructed **not** to run `finish-task.sh`, and the branches were reviewed open — which is also how three wrong plan steps were caught (a `moveItem` payload widening that broke `runBulk`'s `ActionResult<void>`; MCP test fakes that broke 17 tests; schema regexes matching migration **prose** instead of DDL, the third [[2026-08-03-gotcha-72-a-global-regex-with-test-makes-a-guard-silently-blind]] instance). One of those agents noted it plainly: *a verification command scoped to `src/lib/boards/` would have shipped this.*
- **The MCP can now attach files — 24 tools, and the Files column is no longer read-only** ([[2026-08-07-2032-mcp-column-metadata-and-attachments]]). Hermes reported the gap precisely: the surface exposed the Files column but had **no binary upload operation**, and its OAuth credential can't authenticate the web app, so there was no safe workaround. `attach_file` takes inline base64 under 128 KB or a `storagePath` from `create_attachment_upload`'s 2-hour signed URL (50 MB) — and **size and MIME come from Storage, never from the caller's claim**. `get_board` now reports per-column `writable`, `valueShape` and `options` (with ids), so an agent sets a Status cell instead of guessing; an anti-drift suite pins all 18 kinds to the real `cellValueSchema`. The settings surfaced are a deliberate **allow-list**, not the raw jsonb. **Its cross-org RLS suite has never executed** — see [[2026-08-07-gotcha-81-a-plan-can-prescribe-a-test-run-a-guard-forbids]]; the security claim rests on live policy evidence instead.
- **The MCP is now a full read surface — 7 tools to 22** ([[2026-08-06-1343-mcp-full-surface-22-tools]]). Read parity across My Work, Time, Goals, Portfolios, Dashboards, Workload and Reports, plus one write verb (`log_time_allocation`). Built by extracting **client-injected cores** (`…Core(supabase, …)`) out of seven cookie-bound query modules, with the existing exports surviving as thin wrappers — the `upsertCellCore` precedent, so no RSC page changed. Two structural rules held throughout and are worth keeping: **MCP never touches the service client** (the RLS-bypassing `*Cached` helpers are unreachable from any tool; owner maps come from a bridged `listOrgMemberProfiles` instead), and **`getClient()` is called exactly once per handler** because each call charges the rate limit and rotates the bridge secret. The consent table is now generated-checked: its sync test runs the real `registerTools()` against a recording stub, so no hand-maintained mirror can drift — it previously listed 7 of 22 tools **and** claimed "a connected client cannot delete anything" while `log_time_allocation` with `secs: 0` issues a DELETE.
- **Manual time entry had never worked, in production, since it shipped** ([[2026-08-06-gotcha-78-postgrest-on-conflict-cannot-infer-a-partial-unique-index]]). `time_allocations` held **0 rows**. Both its unique indexes are **partial**, and PostgREST's `on_conflict=` cannot emit an index predicate, so every `.upsert(…, { onConflict })` failed at plan time with `42P10` — including from the shipped `/time` page. It hid because the action returns `ActionResult` (a toast, not a crash) and the time card's durable local overlay left the typed number on screen after a failed save. Fixed by migration `20260806060855`: a `SECURITY INVOKER` RPC that writes the arbiter in SQL where the predicate is expressible, shared by `/time` and MCP. **The generalisable rule: a partial unique index is unreachable through PostgREST — its upsert must live in SQL.** And: a write path whose only test mocks the database is untested.
- **`/updates` went stale a second time, so the coverage check moved earlier** ([[2026-08-09-2043-changelog-backfill-and-wrapup-coverage]]). 13 days and **10 promotions** (#79 → #91) shipped with no `Changelog:` trailer — four weeks after the identical gap was backfilled once already. [[2026-07-31-gotcha-64-changelog-drift-check-cannot-see-a-missing-trailer]] named the cause (the drift check proves `generated.ts` matches history, never that history carries the trailers it should) and put the answer at the **promotion** seam; a recurrence that fast says one deferred check is not enough. `/wrapup` step 6 now asks it too, fed by a `CHANGELOG COVERAGE` section in `wrapup-context.sh` so step 1 stays one boot call. The load-bearing design detail: it compares **ship dates, not commits**, because an announcement legitimately rides a backdated empty commit — a per-commit check would flag every backfilled feature forever. `origin/main..HEAD` was tried first and is unusable here for exactly the reason `/promote`'s gate broke: squash-plus-heal makes that range 2508 commits. **Gotcha-64's own follow-up — `/promote` printing the trailer-vs-commit diff — is still open.**
- **In flight (2026-08-26): none.** Spec 2b promoted (PR #99); worktrees and branches from this session's 7-item carryover batch are all merged and cleaned up. **Spec 2 was split**: 2b is reference documents only, the memory layer became **Spec 2c**, and the two share exactly one thing — `document-budget.ts`, which 2c must consume rather than re-derive. **Next: brainstorm Spec 2c** (owner-chosen, depth before breadth); Spec 3 (orchestration, `@handle` addressing, the renameable built-in assistant, and the deferred `agent.create`/`schedule.create`) follows — confirmed still unspecced and correctly deferred: a fresh scoping pass on "Personal Agents Phase 2 remainder" this session found `@mentionable` agents in item threads and per-agent principals both explicitly belong to Spec 3, not before it. **E6 Stripe** remains the other open epic, not blocked on this. **Owed:** the two changelog backfill commits (`ee7de01e`, `80b6811f`) are on `develop` but landed *after* this promotion — they ride the next one. **Owed:** `gotcha-55` fires on **every** migration (6 of 6 across 2a and 2b) so budget the reconcile. **Cleared 2026-08-17:** 2a is promoted (PR #96) and its `"use server"` hotfix with it (PR #98, deploy verified `Ready` and aliased to `www.monolith.works`) — that hotfix sat on `develop` for **three days** while production served the break, which is [[2026-08-17-gotcha-92-a-fix-merged-to-develop-is-not-a-fix-in-production]]. The **`/updates` coverage flags are triaged as noise, not debt**: the check compares **ship dates**, so a build-day date is flagged whenever its announcement rides the next day's promotion — 2026-08-10 is announced by `337d87fa` (3 entries dated 08-11) and 2026-08-12 by `87fde807` (5 entries dated 08-13). 2026-08-14 is genuinely unannounced and **deliberately so** (owner ruling: a regression we shipped and fixed ourselves is not `/updates` material). Expect all three to keep appearing until they age out of the 14-day window. Work is split across **two repos**: `monolith-desktop` (see Desktop below) is a sibling checkout with its own gates, not a worktree of this one.
- **The provider & model layer shipped — Spec 1 of 3, LIVE IN PRODUCTION (PR #95, 2026-08-11)** ([[2026-08-11-1501-provider-model-layer-spec-1]]). 13 tasks, 37 commits, +11483/−1542, two new tables: `ai_providers` (registry) and `ai_models` (catalog, refreshed daily from the public Vercel AI Gateway feed at 03:10 UTC). One API key **per provider** across anthropic/openai/google/mistral/moonshotai, any agent pinnable to any model those keys reach, new models appearing **without a deploy**, and the last hardcoded provider constants gone — which is what had made Mistral and Kimi unreachable regardless of whether a key existed. **The Gateway's id namespace is not the providers'** ([[2026-08-11-decision-35-the-gateway-id-namespace-is-not-the-providers]]): it publishes `claude-haiku-4.5` where Anthropic wants `claude-haiku-4-5`, and only `claude-sonnet-5`/`claude-opus-5` happen to match, which is why nothing failed for months — found when a refresh **retired the model `item_assist` routes to**. Each provider now judges its own ids and unmatched rows are quarantined, so `ResolvedModel` carries both `model` (catalog key: pickers, pins, ledger) and `requestModel` (wire id: adapters only). Two owner decisions on billing: a null catalog price chains to `FALLBACK_RATES` rather than billing $0, and `FALLBACK_RATES` is a per-component **minimum** so Sonnet 5's introductory $2/$10 — **expiring 2026-08-31** — cannot silently displace the standard $3/$15 it is deliberately billed at. **Consequence to expect, not a bug:** google/mistral/kimi pickers are empty until a key is saved for them, because a provider's model list is only verifiable with that provider's own key.
- **Five tests that could not fail, in one plan** ([[2026-08-11-gotcha-89-five-tests-that-could-not-fail-in-one-plan]]). An arg-blind query fake (twice — the second time on the RLS-bypassing service client, where `.eq("org_id", …)` was the **only** tenant boundary); a `costToCredits` assertion satisfied by a naive `×100` with no rounding; a source scan a **comment** could satisfy; and a "never renders a raw key" test asserting on a string never supplied to the render. **Not one was caught by the suite going red** — all five by a reviewer reading the test against its own title. Four repaired in the final fix wave; **six more arg-blind fakes remain outside this branch's files**, two on service-client writes. The rule that closes it: a hand-rolled Supabase fake must **record *and apply*** its predicates, so a missing filter changes observed state rather than a recorded argument (`src/test/ai-models-fake-client.ts` is the shape) — and any test guarding a specific defect must be mutation-tested, not asserted.
- **A leak whose collector is forbidden from where it leaks** ([[2026-08-10-1659-test-account-purge-and-e2e-guard]], [[2026-08-10-gotcha-88-a-leak-whose-collector-is-forbidden-from-where-it-leaks]]). `/admin/users` held **74 profiles of which 14 were real people** — 56 were E2E throwaways (`e2e-offline-…`, `diag3-…`, `probe2-…`, `navmodes-…`), each dragging its own single-member org. `global-teardown.ts` is the sweeper written for exactly this and had never run: it refuses any project not marked `PULSE_TEST_DB`, which is **correct** (the purge is destructive and DEV holds the real live data), while every `e2e/` spec loads `.env.local` — DEV — and provisions via the service-role admin API gated on nothing but "are the secrets present?". **Provisioning was permitted exactly where collection was forbidden**, so the pile could only grow. Purged (verified: profiles 74 → 18, orgs 71 → 15, `auth.users` in sync, Synapse's 429 items untouched), then a second pass took the **duplicate onboarding orgs** — personal orgs created at signup by people who then joined Synapse Solutions — from 15 to **9**, deleting only where an org was empty **and** its owner kept another one. Three empty orgs deliberately survive because each is its owner's **only** org: an empty org that is someone's only org is not a duplicate, it is their account. Orgs holding real client work (Accenture's two Hassantuk boards, 43 items) were never candidates and the refusal moved to the producer: one `globalSetup` choke point wired into **both** playwright configs — `offline.spec.ts` runs only from the second and was the largest single offender. PROD refused outright, DEV needs a deliberate `PULSE_E2E_ALLOW_DEV=1`. **Three `@example.com` accounts must never be swept** — the Tier-2 fixtures, whose deletion does not fail the isolation suite but *silently empties* it (proven safe after the purge: `test:fixtures` 56/56 against DEV) — plus `pulse-autopilot@pulse.internal`, looked up **by email** by later migrations. `/admin/users` now collapses those 4 behind a `<details>` accordion, classified **by domain** (RFC 2606 reserved + `.internal`) rather than an id list that would rot on the next seeded fixture. The generalisable rule: **when you deny-list an environment for a destructive cleanup, ask immediately what is still allowed to write there** — a one-sided guard converts a self-healing leak into a permanent one, and prints reassurance on every run while the pile grows.
- **Report Builder v2 shipped — "Planning & insight" is now 100%** ([[2026-08-09-2130-report-builder-v2-rollups-templates]]). A report is no longer one board: `reports.board_id` became a nullable *home board* pointer, `report_boards` is the canonical membership (backfilled), and `reports.scope` (`board|boards|portfolio|template`) is check-constrained so an inconsistent binding is unrepresentable. A report binds to one board, an explicit set, or a **portfolio it auto-follows**; org **templates** are `scope='template'` rows, so save-as-template needed no second table. New top-level `/reports` owns the index + gallery and the builder moved to `/reports/[reportId]`, with the board-nested URL redirecting. **The estimate of ~1.4k lines of test churn did not materialise, because the per-board shapers were left frozen**: `shapeReport`/`computeKpis`/`computeChartSeries` are byte-identical and a new `aggregate.ts` + `render-data.ts` pool them, so the two highest-risk suites (533 lines) never changed. `render-data.ts` is the preview/PDF parity contract — both paths derive through it, which is why an export cannot disagree with what the user approved. Access mirrors the house `can_edit_portfolio` precedent (creator ∨ org owner/admin ∨ editor-on-all-boards); read needs **one** readable bound board, and unreadable boards are **disclosed** ("N boards omitted"), following the portfolio precedent rather than failing closed. MCP's per-report board probe became one batched `.in()` set-read, preserving the "exactly one probe" assertion. **gotcha-55 fired again on the migration — the ninth consecutive `apply_migration` mis-stamp.** Promoted to production the same day via **PR #93**.
- **Attachments are a real work surface now, and colour is the second sanctioned exception** ([[2026-08-09-1910-file-icons-and-preview-viewer]]). The Files column paints each type in its conventional colour (PDF red, Word blue, Excel green, PowerPoint orange) via a new `--file-*` token palette — a **deliberate scoped exception** to pulse-ui's monochrome-chrome rule, joining status pills; a monochrome chip shipped first and could not tell a deck from a spreadsheet at 24px. The modal sizes itself to content, and **DOCX and XLSX/CSV render inline with nothing leaving our infrastructure**: DOCX parses in-browser inside an iframe with `allow-same-origin` and deliberately **no** `allow-scripts`, while spreadsheets parse **server-side** by reusing the import wizard's already-hardened `parseWorkbookSheets` — so no spreadsheet parser reaches the browser and adding SheetJS would have duplicated a parser the repo owns. PPTX is deferred (every in-browser renderer surveyed is immature and echarts-heavy). **Three of the defects were mine and all were layout or environment, not logic** — `sm:max-w-sm` survived tailwind-merge and clamped the "bigger" modal to 384px, *narrower* than before; a grid pane's auto-sized row let the viewer reach 7272px inside a 616px box so nothing scrolled; and PDFs were rasterised at CSS pixels, halving resolution on every Retina display. The reasoned fix for the scroll bug was wrong and a headless-Chrome measurement of the shipped markup is what found the right one — **jsdom cannot measure layout, so a browser harness over the compiled CSS is the only honest check** (and it can only test classes Tailwind actually emitted).
- **A caching service worker must never run in development** ([[2026-08-09-gotcha-85-a-cache-first-service-worker-poisons-turbopack-dev]]). `sw.js` is cache-first for `/_next/static/**` on the premise that those filenames are content-addressed — true for a production build, **false under Turbopack dev**, which reuses a chunk's filename across recompiles (measured: same URL, 76,036 → 76,124 bytes). It was registered unconditionally, so a dev browser pinned itself to stale chunks and the app died with "module factory is not available" across many *unrelated* modules — surviving dev-server restarts and `rm -rf .next`, because the staleness lives in the browser. Now production-only, and it **unregisters** in dev so an already-poisoned browser heals itself. The portable lesson: **when a symptom survives destroying and rebuilding all server state, stop investigating the server.** Offline behaviour is consequently only testable against `pnpm build && pnpm start`.
- **Invitations were the one collaboration surface that was pull-only, and it took two missing server-side conditions plus one client one to hide it** ([[2026-08-07-2016-invite-realtime-delivery]]). An org invite reached the recipient's bell only on their **next full page load** — reported as "5+ minutes", but there was no timer: `org_invitations` was absent from the `supabase_realtime` publication AND had no invitee SELECT policy (all three policies were admin-scoped; invitees read only through the `SECURITY DEFINER` RPC), while `useInvitations` held `staleTime: Infinity` with `refetchOnWindowFocus` off globally. The durable rule is [[2026-08-07-gotcha-80-realtime-delivery-needs-a-recipient-select-policy]]: **`postgres_changes` evaluates RLS per subscriber, so the recipient policy IS the delivery rule** — publication membership alone pushes nothing — and **`staleTime: Infinity` is only correct when paired with a push channel**; alone it is a one-shot read. Two live-DB facts made the fix cheap and were checked, not assumed: every status transition is an **UPDATE, never a DELETE** (so INSERT+UPDATE covers sent/revoked/accepted/declined on default replica identity), and `org_invitations_email_idx` on `lower(email)` already existed. Still open: an invitee **who already has an account gets no email at all** — `inviteUserByEmail`'s "User already registered" is swallowed by design at `admin-actions.ts:144`.
- **Offline read-only boards shipped, and needed seven fixes after passing every gate** ([[2026-08-06-1423-offline-boards-work-end-to-end]]). Twelve per-task reviews and 4407 unit tests were green over a feature that did not work at all; **every** defect was found by running it in a real browser, and two only because the owner tested the merged result by hand. The durable ones: the SW precached the `/offline` **document and none of its JS** (28 of 30 chunks absent); `persistQueryClientSubscribe` performs **no initial save**, so nothing ever reached disk; TanStack's `onlineManager` **never reads `navigator.onLine`** — a page *loaded* offline reports online forever, which silently disabled both the banner and `assertOnline()`; `fetch()` in the worker is answered from the HTTP cache, so a "network-first" race is **not** a test of connectivity; and each save **overwrote** the whole persisted record, so any reload destroyed every other cached board. Also: a test probe that aborted an IndexedDB `onupgradeneeded` was **manufacturing** one of the symptoms ([[2026-08-06-gotcha-79-a-test-probe-can-manufacture-the-bug-it-is-hunting]]) — a control run with the instrument removed is what separated the two. **Offline acceptance runs against a production build** (`pnpm e2e:offline`); it is structurally unobservable under `next dev`, which serves documents without `no-store`. `finish-task.sh` now installs in the main checkout when a merge changes dependencies, and `playwright.config.ts` no longer silently reuses a `:3000` server from another worktree.
- **Desktop — Plan 2 is BUILT and the app is installable** ([[2026-08-09-1702-desktop-drag-fix-and-pr89-promote]]). All 8 tasks shipped in the sibling repo `~/Dev/monolith-desktop` (deliberately NOT a package here — a pnpm workspace at the root would change `pnpm install` semantics in every task worktree and endanger `start-task.sh`/`finish-task.sh`). Task 4's one companion change landed here; the download lives at **Settings → Integrations → Desktop app** and is live in production. `pnpm install:local` builds straight into `/Applications` and is the daily loop — no Apple account needed. **Two defects survived all eight per-task reviews and every gate**: the window could not be dragged at all (`titleBarStyle: "hiddenInset"` removes macOS's only free draggable region, and the renderer is a web app declaring no `-webkit-app-region: drag`), and `hardenedRuntime: true` with no identity made Gatekeeper report the app as **corrupt** rather than unsigned ([[2026-08-09-gotcha-83-an-unsigned-hardened-runtime-build-reports-as-corrupt-not-unsigned]]). Both were invisible to typecheck/lint/test/build and found only by running the installed artifact — the offline-boards lesson, again. Note `com.apple.quarantine` is stamped by the **downloading browser**, not the build, so this is not an Electron trait: a Tauri app downloaded the same way is blocked identically. **The update feed is now live** ([[2026-08-09-1703-desktop-release-feed]]). `releases.monolith.works` is a zero-build Vercel project that only **307**-redirects `/desktop/*` into the public `desktop` bucket — the indirection is the point, because `publish.url` is compiled into every binary and **can never change for installs already in the field**, and a DEV→PROD cutover is planned; 307 not 308 so the target stays repointable, redirect not rewrite so ~115MB updates come off Supabase egress. 1.0.0 is published and verified end-to-end: found, arm64 **zip** selected over both DMGs, 118MB downloaded, sha512 checked. The missing piece was never the feed — the mac build had **no `zip` target**, and `MacUpdater` does `findFile(files, "zip", ["pkg","dmg"])`, so a dmg-only feed is valid, serves a 200 and is inert ([[2026-08-09-gotcha-84-a-dmg-only-update-feed-is-inert]]). Bucket MIME allow-list widened by migration `20260807165135`. **Auto-update still cannot INSTALL** — Squirrel.Mac validates an update against the running app's designated code-signing requirement, which for an ad-hoc signature is the current binary's `cdhash`, so it is refused by construction; the cert and `hardenedRuntime` must move **together** (gotcha-83). Notarization is parked on the $99 Apple Developer account. **So the shipped update path is manual but no longer silent** ([[2026-08-09-1756-desktop-manual-update-path]]): `latestShell` — advertised since the shell shipped and read by **nothing** — now drives a non-blocking Download/Later prompt at boot plus **Monolith → Check for Updates…**, the hard block offers a Download instead of only Quit, and Settings → Desktop app documents the manual steps (quit first: macOS refuses to replace a running `.app`). Bumping `latestShell` IS the release lever; `minSupportedShell` stays a floor for broken builds only. Note **one manual reinstall bootstraps each user** — the prompt lives in the shell, so copies installed before it cannot announce the version that adds announcing. **1.0.1 is released and live end to end** ([[2026-08-09-2035-desktop-101-release]]), the first release cut since the version stopped being a hand-written constant. `SHELL_VERSION` was a literal `"1.0.0"` in config.ts while package.json carried its own, and NOTHING synced them — package.json drives the artifact names, latest-mac.yml and Info.plist; the literal drove the gate and every prompt comparison. Shipping 1.0.1 would have produced a build reporting itself as 1.0.0, offering users an update to what they already had, forever, with About and the update dialog disagreeing inside one app. The version now comes from `app.getVersion()`, injected into `checkVersionGate(current)` rather than imported so the comparison stays unit-testable without pulling in Electron; the packaged Info.plist was checked at 1.0.1 before publishing. The preload also exposes `version` (via `additionalArguments` — a sandboxed preload cannot reach `app`), so Settings reports **which version you are running** beside the latest. **A publish-script bug surfaced and was fixed in the cutting** ([[2026-08-09-gotcha-87-a-read-straight-after-a-write-is-not-a-verification]])**:** every payload uploaded and the index write returned 200, yet the next read served the PREVIOUS version with a cache-control this script never sets — a single read straight after a write is not a verification. It now polls, cache-busted, and diffs the served feed against `dist/` byte-for-byte. Without that guard the release would have shipped 1.0.1 payloads behind a 1.0.0 feed. **The repo now has a remote, and the signed release has a switch** ([[2026-08-09-2323-desktop-remote-and-release-ci]]). `Synapsekw/monolith-desktop` is **private**, `main`, 20 commits — remote HEAD verified identical to local, so the "16 commits on one disk including a published release" exposure is closed. A **`v*` tag push is now the release**: `.github/workflows/release.yml` builds, signs, notarizes, verifies and publishes, and is deliberately useful *before* there is an Apple account — with no secrets it builds unsigned and uploads artifacts, and **adding the six signing secrets is the only change needed** to make it a real release (no code edit, no flag, no yml change; runbook in `.github/RELEASING.md`). Verified by actually running it (`dry_run`, conclusion **success**): unsigned steps green, all three signing-gated steps correctly skipped, keychain destroyed, 465MB artifact — and the first ever clean-machine run of `install --frozen-lockfile`/`typecheck`/`test`. **`pnpm package:signed` now exists**; `electron-builder.yml` had advertised it since the shell was scaffolded while `package.json` never defined it, and the README filled the gap with the *dangerous* advice to hand-edit `mac.identity` and `hardenedRuntime` — one atomic change spread over two edits, which is how gotcha-83 shipped. Both are now CLI overrides in a single command, so one cannot be applied without the other. **Three traps were latent behind `identity: null`**, all verified against the installed `app-builder-lib@26.15.3` and all of which would have fired on the *first* genuinely signed build: (a) **pairing identity with hardenedRuntime does not actually guarantee signing** — `reportError` only `log.warn`s and skips unless `forceCodeSigning` is set, so an expired cert or a keychain that imports without yielding an identity silently produces the corrupt hardened-but-unsigned bundle; (b) **double notarization** — electron-builder 26 runs its own `@electron/notarize` pass off the *same three* env vars `scripts/notarize.cjs` uses (`notarize: false` now disables it); (c) **`mac.identity` is tri-state** and `adhoc-sign.cjs` read it as a boolean, so on `undefined` both it *and* electron-builder stand down and the linker-signature-only bundle ships. `tests/signing-config.test.ts` turns each invariant into a test (suite 23 → **33**), each **mutation-tested** red — because the comments already explained all of this and a comment cannot fail a build. **Still open there:** the **$99 Apple Developer account** is now the only blocker — nothing has been signed or notarized, and everything from cert import to whether auto-update *installs* is untested by construction. Entitlements worth trimming were found but **deliberately not applied** (it is the one change that can crash the app on launch, and only in a signed build). **One manual reinstall still bootstraps each user** — 1.0.0 predates the prompt and does not expose `version`, so it can neither announce 1.0.1 nor populate the Settings line.
- **Next: nothing is undeployed — `develop == main == production`.** The 1-arg `ai_credential_get` exposure is **CLOSED** by PR #95: production now runs the per-provider reader, so a second provider key is safe to add. Two **post-promotion migrations** are now unblocked and can be batched: drop the unused 1-arg `ai_credential_get` / `org_ai_secret_get` overloads (deployed production no longer calls them), and add `CHECK (>= 0)` on the four `ai_models` price columns (`usablePrice`/`perMtok` are the backstops meanwhile). **First, walk the numbered test guide** in [[2026-08-11-1501-provider-model-layer-spec-1]] against production — step 5 (save a *second* provider key) is the one path no automated test could prove. Expect google/mistral/kimi pickers to be empty until a key exists for each; that is configuration state, not a fault. PR #93's Report Builder v2 guide in [[2026-08-09-2130-report-builder-v2-rollups-templates]] is still unwalked. The `monolith-desktop` **git remote is done** (2026-08-09, [[2026-08-09-2323-desktop-remote-and-release-ci]]) — private repo, 20 commits, plus tag-triggered release CI; that track is now blocked on **buying the $99 Apple Developer account**, not on code, so it is the owner's move and not schedulable work. Strongest candidate otherwise: settle the one open item that may be affecting users **today**: `number`/`battery`/`completion`/`health` dashboard widgets call `SECURITY DEFINER` RPCs through the **service client**, where `auth.uid()` is null and the `can_read_board` guard denies — verified on DEV that all five carry the guard and that it returns false with no auth context, so those widgets should be raising `42501` since it landed 2026-07-04. Chart/list widgets use the bridged client and are fine. **Verify in the running app before "fixing" the guard** — and note `src/lib/dashboards/queries-cached.ts`'s doc comments still claim the service client bypasses RLS as the tenant boundary, which those guards ended. **Spec 2** (`create_dashboard`, `add_widget`, `update_goal`) is now unblocked — its cores are extracted. **Report Builder v2 is DONE** (2026-08-09) — the critical path is now the **E6 Stripe track** (units B, C, E–H), which is **not** blocked on credentials — only end-to-end verification is, and it needs **no migration**, which makes it the safest large task to run beside a migration-bearing one. Personal Agents Phase 2's remaining half — `@mentionable` agents in item threads, agent-initiated proposals, per-agent principals, PDF documents — is specced and unscheduled. **F17** (usage dashboard + AI weekly-digest narrative) survives only in the superseded July E6 plan and is tracked nowhere else.
- **Owed — tooling that lies. CLOSED 2026-08-04** ([[2026-08-04-2140-carryover-repairs]], `c04dfce9`). (1) `reconcile-migration-version.sh`'s zero-row `UPDATE` — **fixed**: the repair now keys on `version` alone (the primary key; STEP 1 already proves identity) and `RETURNING version, name` makes an empty repair visible. The old `AND name = '<slug>'` was wrong because the ledger's `name` column has **two** legitimate forms — measured on DEV: **100 rows `<slug>`** (Supabase CLI `db push`) and **31 rows `<version>_<slug>`** (MCP `apply_migration`) — and the repair only ever runs against the MCP kind. (2) The `rm -f _draft-*.md` before the clean-tree check — **fixed**: extracted to `scripts/clear-untracked-drafts.sh`, which removes only what `git ls-files --others` reports, so a committed draft survives. (3) The orphaned vitest — **fixed**: `scripts/sweep-orphaned-vitest.sh` runs from `finish-task.sh`'s EXIT trap (abnormal paths) **and** explicitly after the gates (the success path removes the worktree, taking the sweep script with it). Scoping is measured, not assumed: on macOS/pnpm 10/vitest 4 the runner **and every worker** carry the worktree's absolute `node_modules` path in their argv, so a sibling worktree's concurrent run is provably untouched. All three are pinned by **behavioural** tests — a temp git repo, real processes, real script output — and each was mutation-checked against the old code before landing: 7 of the 15 assertions went red, the other 8 correctly stayed green. (4) Windows "Directory not empty" on worktree removal has **two** causes and the error text tells you which — *"used by another process"* is a real lock, *"Could not find a part of the path …"* is the `MAX_PATH` limit on deep `node_modules`, fixed only by `cmd /c rd /s /q "\\?\<path>"`. **Reference, not a task.** (5) The four stale remote branches were **already gone** — `git branch -r` shows no `task/*` at all; the entry had outlived the cleanup. (6) **`finish-task.sh` aborted mid-run on macOS bash 3.2 — fixed 2026-08-09** (`0ca47f6b`; [[2026-08-09-gotcha-86-bash-3-2-swallows-a-multibyte-char-into-a-variable-name]]). bash 3.2 absorbs a following multibyte char into a variable name, so `"$MAIN…"` under `set -u` died with `MAIN?: unbound variable` — **only** on a merge that changed dependencies, and **after** the merge and push had landed, so it skipped the `pnpm install` in the main checkout *and* the warning telling you to restart your dev server. Braces (`${MAIN}…`) fix it; `grep -rnP '\$[A-Za-z_][A-Za-z0-9_]*[^\x00-\x7F]' scripts/*.sh` keeps it closed, and our scripts use `→ … —` in operator output so this is a standing hazard. Note `bash -n` cannot catch it, and **`finish-task.sh` is itself unguarded infrastructure — nothing tests it.**
- **Owed — production. Two of three CLOSED 2026-08-04, by evidence.** `digest_secret`'s two halves **match** — proven without touching either secret: `digest_runs` holds 22 rows on 2026-08-02 and 22 on 2026-08-03 with status `sent`/`skipped`, and a mismatched secret 401s and writes **no row at all**. (The lone `blocked` row, 2026-07-28, predates `DIGEST_SECRET` being added to Vercel on ~08-01 — that is the *absent* case, which is the only one the route records.) **The Ask Monolith write path is exercised** — on 2026-08-04 13:54:47 UTC an approved proposal moved `QYSEA (ROV)` Robotics → Software, recorded as an `item_moved` row in `item_activities` matching the "Done" outcome turn to the millisecond, and manually reverted 18s later. That run was on a `develop` build; **PR #84 (`82c5a89a`) put the same code on the deployment**, so the caveat is retired — the write path is reachable in production, though no prod-deployment write has been observed yet. Still open: prod has **no `RESEND_API_KEY`** (confirmed against Vercel — production holds only `DIGEST_SECRET`, `AI_PGNET_HMAC_SECRET`, `OPENAI_EMBEDDING_API_KEY`, `ANTHROPIC_API_KEY` and the three Supabase vars), so digest and briefing runs file in-app notifications and send no email — a run recorded as `ran` with no email is expected, not a fault. Note `net._http_response` **self-prunes** (it held one row, minutes old, when checked) — it answers "is the cron failing *right now*", never "did it fail last week". For history use `digest_runs` / `item_activities`.
- **Do not run `/sync-prod`'s data phase without excluding the fixtures.** The guard passes — prod holds no independent orgs or users — but a full replace copies DEV-only rows into production, **including the two Tier 2 fixture accounts whose passwords are committed to the repo**. Schema-only pushes (`supabase db push`) are unaffected and remain the right tool. Note also: any `db push` to `jzsyq…` re-creates its outbound crons via `cron.schedule` and **must be followed by unscheduling them again** until cutover.
- **Left standing on purpose:** **309** symbols exported but referenced only inside their own module (mostly over-export, not dead code), and the `as unknown as Json` casts on `.from().insert()/.update()`, which `typedRpc` structurally cannot wrap. Pinned by tests rather than fixed: `archiveBoard`'s non-owner message says "delete"; `reorderColumn`/`resizeNameColumn` delegate org-scoping entirely to RLS; `getBoardStatusColumns` drops a column's whole option list when one option fails validation; `deleteBoard`/`purgeBoard` free Storage after the row delete, orphaning objects if cleanup throws. 29 dependencies behind, all patch/minor.

### Last session

```dataviewjs
const sessions = dv.pages('"vault/sessions"')
  .where(p => p.type === "session" && p.status === "complete")
  .array();
sessions.sort((a, b) => String(b.file.name).localeCompare(String(a.file.name)));
const latest = sessions[0];

if (!latest) {
  dv.paragraph("_No finalized session notes yet — capture one at the end of a working session._");
} else {
  dv.paragraph(`**[[${latest.file.name}]]** · _${latest.date}_`);
  dv.paragraph(`![[${latest.file.name}]]`);
}
```

### Recent sessions (last 10)

```dataview
TABLE branch, date as "Session"
FROM "vault/sessions"
WHERE type = "session"
SORT file.name DESC
LIMIT 10
```

### Recent activity — specs/docs (last 14d)

Any spec or doc touched in the last two weeks (whole repo is the vault now).

```dataview
TABLE type, status, file.mtime as "Updated"
FROM "docs" OR "specs"
WHERE file.mtime > date(today) - dur(14 days)
SORT file.mtime DESC
```

## 4. Engineering guardrails (from spec §8)

- TS strict, no unjustified `any`, **Zod at every boundary**.
- **Server Components by default**; Client only when interactive; **Server Actions for mutations**.
- **RLS is the real security boundary** — never trust the client. Default deny on every table; policies key off `org_members` for `auth.uid()`; no cross-org access.
- `SUPABASE_SERVICE_ROLE_KEY` never reaches the browser.
- All schema via **versioned migrations** (never dashboard click-ops). After each migration: `generate_typescript_types` → `src/types/database.types.ts`, then run `get_advisors`.
- Every feature ships with at least basic tests. **No phase complete with failing tests or advisor warnings.** Small conventional-commit commits.
- This is **Next.js 16, not the version in training data** — read `node_modules/next/dist/docs/` before writing framework code (see `AGENTS.md`).
- **The production deployment runs the DEV database** (`hjqca…`), not the PROD project (`jzsyq…`), until the app is feature-complete. DEV therefore holds real live data — treat it with production care. [[2026-08-02-decision-32-production-runs-the-dev-database]]

## 5. Decision log (last 10)

```dataview
TABLE status, file.cday as "Created"
FROM "vault/decisions"
WHERE type = "adr"
SORT file.cday DESC
LIMIT 10
```

## 6. Entry points

- **Mission-control board (visual):** https://claude.ai/code/artifact/eb984761-bee4-4d1a-b6ba-30c6bc05119c — derived view of this doc + worktrees + sessions; refreshed by `/board` (see `.claude/commands/board.md`) and at every `/wrapup`
- [[product]] — what we're building and for whom
- [[architecture]] — system + code structure, data model
- [[platform-roadmap]] — phase 0–9 detail
- [[specs]] — design spec index
- [[operations]] — runbooks, Supabase/MCP, deploy
- [[memory]] — what lives where (this vault's self-map)
