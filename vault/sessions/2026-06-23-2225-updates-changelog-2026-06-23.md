---
type: session
date: 2026-06-23-2225
branch: develop
trigger: wrapup
status: complete
tags: [session]
related: []
---

# /updates changelog — backfill 2026-06-23 entries

## What changed

- `src/lib/changelog/seed.ts` — prepended **9 entries dated 2026-06-23** for work that
  shipped today but wasn't on the public `/updates` page: Dashboards (new), My Time (new),
  Workload insights (improved), Progress columns (new), Send feedback in-app (new), Reorder
  boards in the sidebar (improved), Names on kanban cards (improved), and two `fixed` entries
  (Clearer column errors, Steadier dashboard widgets).
- First time `fixed`-kind entries appear in the timeline, so the "Fixed" badge now renders.
- Trivial content-only edit made directly on `develop` (no worktree). All four gates green
  (typecheck/lint/test 19✓/build). Committed `036da4b` (seed.ts only) and pushed to `origin/develop`.

## Why

The `/updates` changelog is hand-curated in `seed.ts`; new entries are _meant_ to flow from
`Changelog:` commit trailers into the auto-generated `generated.ts`, but today's features merged
without trailers. `seed.ts` explicitly exists to backfill work shipped "before the convention,"
and it's the file CI does **not** check against git history — so already-merged, trailer-less work
belongs there. The page reads `CHANGELOG = SEED + GENERATED`, so entries render with no other change.

## How to test (for the user)

1. Pull `develop`, run the dev server, open **http://localhost:3000/updates**.
2. The top date group should read **June 23, 2026** with the 9 items above, newest-first.
3. Confirm the **"Fixed"** badge styling looks right on the two fix entries (first appearance of that kind).
4. The rest of the timeline (June 22 and earlier) is unchanged.

## Open threads

- Going forward, route new user-facing changelog copy through `Changelog:` commit trailers
  (`pnpm changelog:gen`) so `generated.ts` stays the source of truth and seed backfills stop growing.

## Next session entry point

Changelog is current through 2026-06-23. Back to the queued work: run `/promote` to ship the
`develop` bundle to `main`, then Phase 9.3 cache + 9.4 skeletons.
